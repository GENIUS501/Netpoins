@model IEnumerable<Entidades.EEnlaces>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <h1>Enlaces</h1> <br />
    <div class="row">
        <div class="col-2">
            <button type="button" class="btn btn-outline-primary" onclick="ClickNew()"><i class='bx bx-add-to-queue'></i>Agregar</button>
            <br />
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-12">
            <table id="GridEnlaces" class="table table-dark table-hover">
                <thead>
                    <tr>
                        <th>Provincia</th>
                        <th>Sitio</th>
                        <th>Unidad Ejecutora</th>
                        <th>Número de Linea</th>
                        <th>IP Gateway</th>
                        <th>Interface</th>
                        <th>Tipo Enlace</th>
                        <th>Bandwidth</th>
                        <th>Medio Enlace</th>
                        <th>Proveedor</th>
                        <th>Comentario</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Netpoints.Controllers.EnlacesController.Oficinas(item.IdOficina.ToString()).Provincia</td>
                            <td>@Netpoints.Controllers.EnlacesController.Oficinas(item.IdOficina.ToString()).NombreOficina</td>
                            <td>@Netpoints.Controllers.EnlacesController.Oficinas(item.IdOficina.ToString()).UE</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).Linea</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).Gateway</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).Interface</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).TipoEnlace</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).Bandwidth</td>
                            <td>@Netpoints.Controllers.EnlacesController.Redes(item.IdOficina.ToString()).MedioEnlace</td>
                            <td>@Netpoints.Controllers.EnlacesController.Proveedores(item.IdOficina.ToString()).NombreEmpresa</td>
                            <td>@item.Comentario</td>
                            <td>
                                <button type="button" class="btn btn-outline-primary" onclick="ClickUpdate(@item.IdEnlace)"><i class='bx bxs-edit'></i>Editar</button>
                                <button type="button" class="btn btn-outline-primary" onclick="ClickDelete(@item.IdEnlace)"><i class='bx bxs-trash'></i>Eliminar</button>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div id="root">

</div>

@section scripts{
    @if (TempData["msg"] == "0")
    {
        <script>
            Swal.fire(
                'Eliminado!',
                'El Enlace ha sido Eliminado..',
                'Exitosamente'
            );
            var IsLoading = false;
        </script>
    }
    else
    {
        <script>
            var IsLoading = true;
        </script>
    }
    <script src="~/Scripts/Enlaces/Index.js"></script>
}
